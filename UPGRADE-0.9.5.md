# Anchor Memory 0.9.5 升级说明

本版只针对手机和平板界面适配，不更改已有记忆数据结构。

- 修复部分 Android WebView 将手机宽度识别为 800–1000 CSS px，导致旧版 520px 媒体查询失效、面板只显示半截的问题。
- 面板打开后始终占满当前可视视口，旋转屏幕、浏览器地址栏变化和软键盘弹出时会重新同步尺寸。
- 背景页面在面板打开时停止滚动；关闭后恢复。
- 标签页改为单行横向滑动，减少手机端顶部占用。

直接覆盖安装即可，原有摘要、锚点、人物、物品、场景和关系数据不会被清空。
