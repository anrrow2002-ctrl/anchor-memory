# Anchor Memory 0.9.7 测试报告

执行环境：Node.js 静态语法检查与纯逻辑回归测试。

## 已执行

- `node --check index.js`
- `node test_am097.mjs`
- 历史测试入口 `test_am092.mjs`、`test_am093.mjs`、`test_am095.mjs`、`test_am096.mjs`
- ZIP 文件完整性测试

## 覆盖点

- 扩展与 manifest 版本一致；
- 默认分段/累计间隔为 15 / 75；
- 缺失摘要警告不存在 `onclick`，启用点击关闭与关闭按钮；
- 间隔修改函数、延迟重新检查和任务启动时快照存在；
- 新锚点/合并保存实际间隔和批次数量；
- 设置页明确说明调整生效边界和不整除处理；
- 0.9.6 动态召回发送前注入、0.9.5 手机全屏、0.9.4 摘要锁定等回归继续通过；
- 5,000 条实体账本压力测试通过。

结果：全部通过。
