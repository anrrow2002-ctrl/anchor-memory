# Anchor Memory 0.9.7 升级说明

可直接覆盖 0.9.6 安装目录，聊天记忆数据版本仍为 11，不需要清空或重建已有记忆。

升级后：

- 点击缺失摘要警告只会关闭提示，不再打开控制面板；
- 新安装默认累计合并间隔为 75；
- 已有设置会保留，因此旧用户原先的 100 不会被偷偷改成 75；需要时可在“运行设置”手动改为 75；
- 修改分段/合并间隔后，从下一批未处理摘要开始生效；旧锚点和旧合并继续有效；
- 正在运行的任务会按启动时的旧值完成，随后自动按新值重新检查。

覆盖升级前仍建议在“工具”页导出一次记忆 JSON。
