# Anchor Memory 0.9.14 自检报告

## 静态检查

- `node --check index.js`
- `node --check core/runtime-controls.js`
- `node --check core/time-engine.js`
- `node --check core/entity-ledger.js`
- 函数声明重名扫描
- 顶层常量重名扫描
- `settings.html` 重复 ID 扫描

## 0.9.14 回归覆盖

- OpenAI Chat Completions、Responses API、Gemini、数组 content、嵌套 data/response、reasoning-only 与 SSE 响应解析。
- HTTP/后端错误、空响应、未知结构和截断状态分层。
- 手机端拉取前同步输入框并清空旧模型；模型列表只接受真实模型 ID，不把供应商名称误当模型。
- 网络/配置失败不进入摘要纠正重写；只有真实 Godlog 文本格式失败才纠正一次。
- 副 API 配置不完整时不运行后台记忆队列。
- 最近三个有效 AI 回合原文窗口、插件管理隐藏 system 行、玩家手动隐藏所有权隔离。
- Godlog metadata/UI-only 与消息正文残留清理。
- 转档副本精简与原聊天加载硬保护。
- 两条主提示词注入钩子的旧 Godlog 清理和单份 Anchor Memory 注入。

## 历史回归

复跑 `test_am0910.mjs`、`test_am0911.mjs`、`test_am0912.mjs`、`test_am0913.mjs`，用于确认新楼关系表、派生记忆追赶、显式重写、归档最终合并和摘要长度校验仍保持。

## 本次执行结果

- 0.9.8 记忆连续性场景：正常90回合、延迟摘要、永久缺失楼层与运行中改间隔均为 0 个记忆缺口。
- 0.9.9 压力场景：1,200 次提示词注入、700 组长聊天连续性、1,500 次动态召回、300 次总开关循环，数据丢失与暂停残留均为 0。
- 0.9.10：2,000 组尾部重建、1,976 组历史中间缺口、3,000 组宿主消息对象替换、2,500 组合并边界全部通过。
- 0.9.11：5,000 组重写级联回滚场景全部通过。
- 0.9.12、0.9.13、0.9.14 专项测试全部通过。

## 边界

自动测试覆盖代码与模拟数据流，不能替代用户具体手机 WebView、反向代理和副 API 供应商的真实联网验证。0.9.14 已把这些环境差异暴露为可区分的错误信息，避免再被统一误报为“没有返回摘要正文”。
