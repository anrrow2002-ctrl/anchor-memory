# Anchor Memory 0.9.11 测试报告

## 检查范围

- JavaScript 语法检查；
- 总览页四个生成/重写按钮是否真实绑定；
- 记忆库逐条重写按钮与选中重写按钮是否真实绑定；
- “立即全量合并”与“重写全量合并”是否彻底拆分；
- 分段锚点重写后的依赖合并回滚；
- 历史全量合并重写后的后续合并回滚；
- 0.9.10 的新楼尾部摘要、源楼临时替换、累计合并追赶等回归项。

## 自动测试结果

运行：

```bash
node --check index.js
node test_am0911.mjs
```

结果：

- 语法检查：通过；
- 显式锚点重写入口：通过；
- 显式全量合并重写入口：通过；
- 卡片逐条重写入口：通过；
- 无新增材料时不再暗中触发重写：通过；
- 事务性依赖回滚模型：通过；
- 随机重写级联回滚：5,000 组通过；
- 最新尾部待摘要重建：2,000 组通过；
- 历史中间缺口识别：1,976 组通过；
- 消息对象替换重新定位：3,000 组通过；
- 累计合并边界模拟：2,500 组通过。

## 事务行为验证

- 重写锚点 `k80` 所在批次时，保留不依赖它的第 1 次合并，回滚第 2 次及以后合并；
- 重写首批锚点时，回滚所有依赖的累计合并；
- 重写第 2 次累计合并时，保留第 1、2 次，回滚第 3 次及以后；
- 重写最近一次累计合并时，不删除该合并本身，也不会新增编号。

## 测试边界

以上为本地代码检查和确定性/随机逻辑模拟，没有实际连接用户的 SillyTavern 与副 API 进行数千次付费请求。真实模型输出质量仍取决于用户配置的模型和自定义提示词。
