# Anchor Memory 0.9.2 FINAL 回归测试

## 自动检查

在扩展目录运行：

```bash
node --check index.js
node test_am092.mjs
```

自动检查覆盖：版本与数据迁移、六段主提示顺序、动态召回默认关闭、提示词字符预算、稳定消息键、关系历史重映射、脏关系禁用、事务式索引提交、重复 Prompt 去除、注入深度 0、并发锁、聊天切换隔离、中文召回行为和设置页重复 ID。

## SillyTavern 本机检查

1. 新开聊天发送 4 个 AI 回合，确认最终请求只保留设置中规定的最近原文窗口。
2. 在 Prompt Inspector 中搜索“锚点记录”，确认只出现一个完整六段系统块。
3. 将注入深度设为 0，确认系统块位于最后一条对话消息之后；再改为 4，确认位置随设置变化。
4. 保持“额外旧楼动态召回”关闭，确认第六段只出现尚未锚定摘要；开启后才可能出现“动态召回”。
5. 连续双击“生成锚点”和“全量合并”，确认只启动一次任务。
6. 副 API 工作期间切换到另一个聊天，确认旧任务返回后不会在新聊天产生摘要、锚点或隐藏状态。
7. 删除中间楼层并返回聊天，确认后续楼层不会全部变成缺失摘要；关系表会回退并等待安全重建。
8. 临时使用一个会返回残缺 Markdown 的副模型，确认人物/物品/场景旧数据保留且状态显示待重建。
9. 工作台关闭时进行长回复，确认流式输出没有因面板全量重绘明显掉帧。
