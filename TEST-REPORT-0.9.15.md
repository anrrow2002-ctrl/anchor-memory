# Anchor Memory 0.9.15 测试报告

## 静态与专项测试

- `node --check index.js`：通过。
- 超时分类：通过；不存在 `abort('secondary-timeout')`、`abort('embedding-timeout')` 或 `abort('model-list-timeout')`。
- 移动端/WebKit 异常形态模拟：通过；原始取消原因不会再显示为 API 错误。
- 分段边界：37 楼样本按顺序完整覆盖，每段不超过 10 楼；除单条超长摘要外，每段不超过 9,000 字符。
- 断点保存与继续：通过；每段成功后游标递增，失败保留候选状态。
- 固定人物关系与人物/物品/场景索引同事务重建：通过。
- 自动退避及连续三次无进展停机：通过。
- 历史污染边界：通过；已索引历史被修改时旧表停止注入，纯尾部新增仍可使用旧安全快照。
- 手动修改/重跑 Godlog 的污染边界登记：通过。
- 归档快照清除运行态重建游标：通过。

## 历史回归

复跑 0.9.8–0.9.14 全部测试并通过：

- 1,200 次提示词注入场景，失败 0。
- 700 组 100–360 回合记忆连续性模拟，记忆缺口 0。
- 1,500 次动态召回时序，晚到且误标为已使用 0。
- 300 次暂停/恢复，数据丢失 0、暂停提示词残留 0、插件隐藏楼残留 0。
- 400 次生成期间暂停竞态，通过。
- 300 次隐藏状态竞态，通过。
- 2,000 组尾部摘要重建，通过。
- 1,976 组历史中间缺口，通过。
- 3,000 组消息对象替换，通过。
- 2,500 组合并边界，通过。
- 5,000 组锚点/累计合并重写级联回滚，通过。
- 90 回合场景：75 回合累计合并与剩余尾段连续性通过。

## 测试边界

自动测试覆盖代码路径、状态迁移与模拟运行逻辑；无法替代用户具体手机浏览器、SillyTavern 版本、反向代理和副 API 供应商的真实联网表现。0.9.15 会把这些外部错误分别显示，便于准确定位。
